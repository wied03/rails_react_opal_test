# https://github.com/opal/opal-rails/issues/49
# There is a difference between sprockets (ensures the code is compiled) and making sure the browser runs it
# this ensures the browser runs it
Opal.require 'foobars_view'